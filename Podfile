# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HT_Shopping' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HT_Shopping
pod "Alamofire"
pod "SwiftyJSON"
pod "SnapKit"
pod "HandyJSON"
pod "MJRefresh"
pod "Kingfisher"
pod "MBProgressHUD"
pod "IQKeyboardManagerSwift"
end
